@page "/categories"

<PageTitle>Categories</PageTitle>

<AuthorizeView>

    <Authorized>
        <div class="flex flex-row justify-center">
            <div class="flex flex-col h-full w-full px-8 py-4 gap-3 text-zinc-600 lg:px-4">

                <h1 class="text-xl italic">Categories</h1>

            </div>
        </div>
    </Authorized>

    <NotAuthorized>
        <div class="flex flex-row justify-center">
            <div class="flex flex-col h-full w-full px-8 py-4 gap-3 text-zinc-600 lg:px-4">

                <p class="text-xl">Categories</p>

            </div>
        </div>
    </NotAuthorized>

</AuthorizeView>

@code {

}
